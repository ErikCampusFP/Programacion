import numpy as np
# Crear el array de ceros de 3 por 3
array = np.zeros((3,3))
# Aqui indicamos que el valor en la posición 1 por 1 (en este caso el centro) es 1
array[1,1] = 1
# Mostramos el array
print(array)
