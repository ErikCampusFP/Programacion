import numpy as np

# Definimos el array
array  = np.array([2, 3, 4, 5, 6])

# Elevamos al cuadrado cada elemento del array con el comando square
arrayAlCuadrado = np.square(array)
print(arrayAlCuadrado)